# Loaded by Vite when running `vite build` (npm run build).
# Empty value → fetches go to the same origin as the page.
# In production the Express server static-serves the SPA, so /api/* is on
# the same origin and no cross-origin URL is needed.
VITE_API_URL=

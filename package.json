{
  "name": "investment-agent",
  "private": true,
  "version": "1.0.0",
  "description": "Personal investment agent: per-call analyst (v6 Anthropic prompt) + trend layer (v2.1) + 3-axis classifier + advisory feed.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "cd server && npm ci --include=dev && cd ../client && npm ci --include=dev",
    "build": "cd server && npx prisma generate && cd ../client && npm run build",
    "start": "cd server && node index.js"
  }
}

# corpus-archive — findings log

- **Step 0 sha256**: computed sha256 of `~/Dropbox/LUIS/Backups/investment-agent-backups/analysis_corpus_20260830.sql` = `a5df033cb6c6cc20f493bccbb9f86b28a73b0f210d7fdbb254411caaa8896923`. Matches the prompt's stated value exactly (both 64 hex chars). No mismatch.
- **Step 0 table scope**: dump contains exactly `INSERT INTO public."Ticker"` / `"Transcript"` / `"Analysis"` — no other tables. Counts: 45 / 708 / 805. Total 1558 INSERT statements — matches prompt claim exactly.
- **Step 0 completeness**: dump ends `-- PostgreSQL database dump complete`; `\restrict` and `\unrestrict` tokens both equal `SU5fWSsysxkRjZfq5hc0HljsxLA7ZNzqkuOHre2TJEIIPLFNQmMuZ3BBziMadge`. Match.
- **Step 0 reconciliation vs §4.4**: live DB (2026-09-04) has 45/709/806 (Ticker/Transcript/Analysis) — one more Transcript, one more Analysis than the dump. Consistent with "corpus grew ~1 row since." State-of-play §4.4's 42+764=806 Analysis figure matches the *live* count, not the 20260830 dump's 805 — the dump predates §4.4's reconciliation work, as the prompt anticipated. Not resolved here, reported per prompt instruction only.
- **Step 0 pg client**: local pg_dump/pg_restore/psql/createdb/dropdb present via homebrew libpq, client v18.4. Live server v18.6.
- **Step 0 read-only role**: `corpus_backup_ro` already existed from a prior session (SELECT on Ticker/Transcript/Analysis), credential stored as `CORPUS_BACKUP_DATABASE_URL` in `.env`. Verified it can SELECT and cannot INSERT (permission denied confirmed live). Found and fixed a gap: the role lacked SELECT on `Ticker_id_seq`/`Transcript_id_seq`/`Analysis_id_seq`, which `pg_dump --data-only` needs to read current sequence values -- first dump attempt failed with "failed to get data for sequence Analysis_id_seq". Granted SELECT on the three sequences (catalog-only write, same exception class as the CREATE ROLE itself).
- **Step 2 dump script executed for real** (`scripts/dump_corpus.sh`, already present from a prior session, reviewed and matches prompt spec): produced `analysis_corpus_20260904.sql` (43,326,798 bytes), schema file, sha256, and census. Live counts at run time: 45/709/806 (Ticker/Transcript/Analysis) -- matches dump exactly. Wall time ~25s.
- **Step 3 real restore test performed**: created local scratch DB `corpus_restore_test` (local Postgres 18.6 via homebrew), restored schema then data. Two non-fatal errors during schema restore, both from local-environment role mismatches, not corpus defects: (1) `ALTER TABLE ... OWNER TO postgres` failed until a local `postgres` role was created to match Railway's; (2) a `GRANT ... TO corpus_backup_ro` statement in the schema dump failed because that role doesn't exist on the scratch DB. Both are artifacts of `pg_dump`'s default behavior of including ownership/ACL statements -- worth noting as a restore-script wrinkle (a restore onto a fresh Postgres instance needs `--no-owner --no-acl` or matching roles pre-created), not a defect in the archived data. After both failures, table creation and ALL data rows loaded successfully. Post-restore counts: 45/709/806 -- exact match to live DB. Scratch DB and the temporary local `postgres` role were dropped after the check.
- **Step 3 restore re-verified after fix**: added `--no-owner --no-acl` to the schema `pg_dump` call in `scripts/dump_corpus.sh` (the prior restore's two errors were both artifacts of ownership/ACL statements referencing roles that don't exist on a fresh target, not corpus defects). Re-dumped schema with the fix, restored into a second scratch DB (`corpus_restore_test2`): schema exit=0, data exit=0, zero errors. Post-restore counts 45/709/806, exact match to live. Scratch DB dropped.
- **Step 1 was already done in a prior session** (not by this session): repo `Luis-GrowSolar-Personal/investment-agent-corpus`, private, seeded commit `cb7f94c` (2026-09-05T02:40:19Z), contents match spec exactly (`corpus/`, `caches/20260830/`, README.md), sha256 of the seeded data file verified to match the known-good `a5df033c...` exactly, uncompressed plain SQL as required. **Finding, flagged in report**: the seed commit's author/committer identity is `Luis Morales <l.morales@windmarenergy.com>` (the Windmar work email) even though pushed to the `Luis-GrowSolar-Personal` account -- CLAUDE.md states this project is "Personal account — Luis. Separate from Windmar Energy entirely." Not amended here (out of this run's scope to rewrite pushed history), reported plainly instead.
- **Step 1 README bug found and fixed**: it described `.github/workflows/corpus-archive.yml` in the present tense as already scheduling pushes, but Step 4 was not built (in the prior session or this one) -- confirmed no `.github/` directory exists in the archive repo. Corrected via commit `7eb9254` to describe the workflow as designed-but-not-yet-built. This was a false claim in a document meant to be citable; leaving it would have been the "silent failure that manufactures confidence" the prompt warns against.
- **Step 4 (GitHub Actions) explicitly deferred this session per direct user instruction** -- not attempted. Requirements captured in the wrap-up for a follow-up session: weekly `schedule:` + `workflow_dispatch:`, `DATABASE_URL` repo secret using the `corpus_backup_ro` read-only credential (already exists, see Step 0 notes), PGDG apt install of `postgresql-client-18` on `ubuntu-latest` (default client is older and pg_dump will refuse to dump the live 18.6 server), commit-only-on-checksum-change (script already implements this locally), fail the job on any Step-3-class verification failure.
- **Step 5 done**: one paragraph added to `docs/architecture/BUILD_STATE.md` under Open Questions describing the deferred corpus-integrity read-only endpoint, per prompt's exact scope (no route, no server changes).

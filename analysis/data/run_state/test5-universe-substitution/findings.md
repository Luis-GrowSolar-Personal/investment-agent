## Step 0 flag 1 -- prompt's Step 1 'expected' figure is wrong (load-bearing)

The prompt says: run q=0.0 uncorrupted, 'Expected: final_value = $120,800 (Test 1's own zero-information floor... cell_key=="zero_info")'. Verified directly against `analysis/data/run_state/analyst-sensitivity/cells.jsonl`: `zero_info` cells (recommendation force-set to Hold/Intact, size=None -- genuinely NO information) all read final_value=$120,799.82, max_dd=12.72%. But the cells actually named `<mode>_q0.0` (real recommendations, q=0 means 'no corruption applied', which is what 'uncorrupted' means everywhere else in the prompt and in state-of-play itself) all read final_value=$179,944.91, max_dd=20.85% -- this is the figure state-of-play §5.2 calls the 'uncorrupted result' and the one Test 1's own hard-stop gate text (quoted verbatim by this prompt) is about. These are two different, deliberately different cells in Test 1's own design -- 'uncorrupted real recommendations' and 'zero information' are not the same concept, and the prompt's Step 1 conflates them. **Used $179,944.91 / 20.85% as Arm A's reproduction target, not $120,800** -- it is Arm A's actual point (the real, already-deployed portfolio's settled-config result), consistent with the prompt's own framing of Arm A as 'the real portfolio', which carries real recommendations, not zero information.

## Step 0 flag 2 -- the named 'ESTABLISHED/SPECULATIVE source of truth' file doesn't define that constant; tier is dynamic, and AMD doesn't match its own label (load-bearing)

The prompt's premise check #2 asks to verify the ESTABLISHED/SPECULATIVE ticker-list constant is identical across its ~15 copies, and 'use the definition in analysis/sweep_cadence_and_session_model.py... as the source of truth for this run.' Verified: that file defines no such constant at all -- only `ALL16` (a flat 16-ticker list, no established/speculative split) and a `tier_fn` built by `trend_analyst.build_tier_function()`, which computes each ticker's tier PER-TICKER, LIVE, from cached market cap / trailing P/E / trailing volatility (2-of-3 axes fire -> 'speculative'), independent of any hand-curated list. The 7 other files' `ESTABLISHED = [...]` constant is not wrong or drifted (all 7 identical), it is simply not what this pipeline's tier logic reads.

**Consequential finding, not caused by this test**: computing `tier_fn` against the live caches for all 16 ALL16 names plus AMZN/META/V/JNJ shows `AMD` classifies as **speculative** (fired axes: trailing P/E 133.3 > 50, trailing volatility 62.8% > 50%; market cap $567B alone does not save it), not established, despite being one of Arm A's nominal 'established eight' everywhere in the project's docs and prompts. Every other Arm A/B established name (AAPL, AVGO, GOOGL, MSFT, NVDA, ORCL, TSLA, AMZN, META, V, JNJ) computes as established. This means Arm A's real settled-config result already runs AMD at the speculative Type-A cap and speculative starter-position size, and its trend layer applies the faster (1-quarter) upgrade rule -- not the established-tier behavior its label implies -- in every prior sweep on this branch, not just this run. **Effect on Arm A vs Arm B's comparison**: Arm B's eight established names are unanimously tier=established; Arm A's eight are 7 tier-established + 1 (AMD) tier-speculative. Any performance difference between the arms is very mildly confounded by this pre-existing tier/label mismatch on the AMD position specifically -- reported here, not corrected (correcting it would mean silently changing Arm A, which the prompt explicitly says not to do: 'Arm A is the real, already-deployed portfolio and is not adjusted for this test').

## Step 5 -- resampling distribution (25 draws, seed 20260906)

mean=135,121.97, std=17,508.56, min=107,344.08, max=158,346.54. Arm A's real final_value (179,944.91) falls at the 100.0th percentile of this distribution.

## Step 3 -- concentration comparison (final-day position values)

Arm A top 3 by final-day dollar value: NVDA 24.0% + AVGO 20.3% + ORCL 16.2% = 60.5% of final_value, all three semiconductor names, none present in Arm B. AMD (also semiconductor) adds another 7.7%, so semiconductor exposure alone is ~68% of Arm A's final value. Arm B top 3: TTD 17.4% + V 17.2% + FSLR 12.2% = 46.8%, no single position above 17.4%, zero semiconductor names (the swapped-out AMD/AVGO/ORCL/NVDA are exactly the removed names). Arm A is substantially more concentrated in single-name (and single-sector) outperformance than Arm B.


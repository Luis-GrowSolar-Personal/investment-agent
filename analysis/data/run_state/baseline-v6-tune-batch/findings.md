
- 2026-09-16T01:05:37Z: Coverage miss (transcript): GIS 2026Q1 call_date=2025-09-17 status=404

- 2026-09-16T01:46:02Z: Coverage miss (transcript): SRE 2020Q1 call_date=2020-05-04 status=404

- 2026-09-16T02:11:00Z: **Bug caught by the pilot, before batch submission**: `all_calls()` in baseline_v6_tune_batch_driver.py originally scanned the ENTIRE shared `transcripts/` directory (train+tune tickers coexist there), not scoped to tune's working-symbol set. The 5-call pilot picked 5 ABBV (train) transcripts instead of tune ones. Cost of this: $0.4321 for 5 calls that duplicate work already scored and committed on train -- pure waste, not corpus contamination (results discarded, not written to any eval cache; renamed to scores_DISCARDED_wrong_ticker_pilot.jsonl, not deleted, for the record). Fixed `all_calls()` to filter by tune's working-symbol set before re-running the pilot. Had this not been caught, `cmd_submit()` would have used the same unfiltered `all_calls()` and the ~$47 batch would have re-scored up to 1,240 already-scored train calls alongside tune ones -- violating hard constraint #1 ("Train split ONLY... Do not score train again") and roughly doubling the real spend.

- 2026-09-16T02:45:00Z: **Coverage guard fired on tune, as a hard stop per the prompt's own rule ("Do not pass --allow-partial-coverage... that is a finding to fix, not to override")**. WOLF: 23/23 calls (100%) lose price coverage -- expected, documented in SCORING_PROTOCOL_TUNE.json before scoring (Yahoo has purged WOLF's pre-Chapter-11 price series entirely). SPWR: 10/15 calls (67%) lose price coverage -- NOT anticipated by the prompt. Root cause investigated: scorer_price_cache_v1.json's SPWR series starts 2023-01-03, not the corpus window start (~2019-2020) -- confirmed via a live yfinance probe that pre-2023 SPWR data is ALSO fully purged from Yahoo, not merely unfetched. This is the same mechanism as WOLF: the original SunPower Corporation filed Chapter 11 in 2024 and the SPWR ticker was subsequently reused by a different, reorganized entity (Complete Solaria's post-acquisition rebrand), so Yahoo carries no history for the pre-petition company under that symbol. scorer_price_cache_v1.report.json marked SPWR "status": "ok" because the fetch itself succeeded -- it never checked whether the returned series covers the corpus window, which is why this gap was invisible until the coverage guard caught it here. Both tickers are corporate-reorg cases the existing A6 terminal-value mechanism was built for, but A6 is not wired into analyst_direct_scorer.py (see SCORING_PROTOCOL_TUNE.json -> wolf_price_coverage). STOPPING per the prompt's explicit rule rather than overriding the guard or inventing a fix mid-run.
